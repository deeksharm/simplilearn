name: simplilearn workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # This step checks out a copy of your repository.
      - uses: actions/checkout@v3
      # This step references the custom action from a GitHub repository.
      - name: Run custom action
      - uses: ./.github/actions
